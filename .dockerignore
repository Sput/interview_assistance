# Reduce Docker build context size and avoid leaking local files
.git
.github
.vscode
.DS_Store

# Next.js build artifacts and cache
.next
.turbo

# Dependencies
node_modules

# Env files (do not send secrets to the build context)
.env*

# Compose and Docker metadata
Dockerfile*
docker-compose*

# Misc
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
README.md
